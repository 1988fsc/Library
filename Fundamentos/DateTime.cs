//Valor monetário, date time

/*using System.Globalization;

CultureInfo.DefaultThreadCurrentCulture = new CultureInfo ("en-US");


decimal valorMonetario = 1561.54M; //para reconhecer decimal com casa colocar M
Console.WriteLine($"{valorMonetario:C}");*/



/*DateTime data = DateTime.Now;

Console.WriteLine(data.ToString ("dd/MM/yyyy HH:mm"));
Console.WriteLine(data.ToShortDateString ());
Console.WriteLine(data.ToShortTimeString ());

string dataSring = "2024-01-04 18:00";

    bool sucesso = DateTime.TryParseExact(dataSring, "yyyy-MM-dd HH:mm", CultureInfo.InvariantCulture, DateTimeStyles.None, out DateTime dateNow);

        if (sucesso)
        {
        Console.WriteLine($"Conversão realizada, data: {dateNow}");
        }
        else
        {
        Console.WriteLine($"{dateNow} não é uma data válida");
        }*/